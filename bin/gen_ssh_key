#!/bin/bash

while [[ "$email" == "" ]]; do
    echo "Input Your email for this key."
    read email
done

if [ ! "$email" = "" ]; then
    ssh-keygen -t rsa -b 4096 -C "$email"
else
    echo "abort..." 1>&2
    exit 1
fi

