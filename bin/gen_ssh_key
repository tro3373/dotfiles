#!/bin/bash

# while [[ "$email" == "" ]]; do
#     echo "Input Your email for this key."
#     read email
# done

email="$1"
cmd="ssh-keygen -t rsa -b 4096"
if [ ! "$email" = "" ]; then
    cmd="$cmd -C \"$email\""
fi
echo $cmd
$cmd

