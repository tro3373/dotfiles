#!/usr/bin/env bash

readonly repo=tro3373/md
readonly dst=$HOME/.md

main() {
  if [[ -e $dst ]]; then
    echo "$dst already exist." 1>&2
    exit 1
  fi
  git clone git@github.com:$repo.git $dst
}
main "$@"
