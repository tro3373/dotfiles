#!/usr/bin/env bash

# Claude Code をブラウザから操作できるクライアントを自作したらめちゃくちゃ捗った
# https://zenn.dev/bm_sms/articles/claude_code_viewer
main() {
  set -eo pipefail
  h=0.0.0.0
  p=3400
  open http://localhost:$p &
  npx -y @kimuson/claude-code-viewer@latest --port $p --hostname $h
}
main "$@"
