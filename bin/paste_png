#!/usr/bin/env bash

has() { command -v ${1} >&/dev/null; }
main() {
  if ! has gclip; then
    echo "No gclip exist" 1>&2
    exit 1
  fi
  if [[ -e img.png ]]; then
    echo "already img.png exist." 1>&2
    exit 1
  fi
  gclip -paste -f img.png
  echo "img.png pasted." 1>&2
}
main "$@"
