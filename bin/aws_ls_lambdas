#!/usr/bin/env bash

main() {
  aws lambda list-functions --query 'Functions[].FunctionName' "$@" |
    tr -d '"' |
    tr -d ',' |
    tr -d '[' |
    tr -d ']' |
    tr -d '	' |
    tr -d ' ' |
    sed -e '/^$/d' |
    sort -u #| tee ls_lambdas.log
}
main "$@"
