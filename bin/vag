#!/usr/bin/env bash

DOTPATH=${DOTPATH:-~/.dot}

check() {
  if [[ ${OSTYPE} != "msys" ]]; then
    echo "Not supported" 1>&2
    exit 1
  fi
}
main() {
  check
  cmd //c start "" $DOTPATH/misc/vagrant/arch/vagrantup.bat
}
main "$@"
