#!/usr/bin/env bash

export minimal=1
# readonly _script_dir=$(
#   cd "$(dirname ${BASH_SOURCE:-$0})"
#   pwd
# )
# source $_script_dir/../funcs_arch

pre_setup() {
  # log "=> pre setting up"
  :
}

load_update_arg_apps() {
  # shellcheck disable=SC2034
  read -a args < <(
    echo "$(default_apps) ahk wt hackgen"
  )
}

post_setup() {
  # log "===> reboot maybe needed."
  :
}
