#!/usr/bin/env bash

install_common() {
  exe cd "$HOME"
  exe npm install -g @anthropic-ai/claude-code
  exe asdf reshim
}
install_mac() {
  exe brew install --cask claude
}

setting_common() {
  log "==> claude"
  log "===> /init"
}
