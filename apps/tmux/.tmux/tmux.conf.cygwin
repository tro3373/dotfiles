bind C-o display-message "Cygwin conf loaded"

# コピーした内容をクリップボードに送る
unbind -t vi-copy Enter
bind-key -t vi-copy y copy-pipe "iconv -s -t SJIS | putclip"
bind-key -t vi-copy Enter copy-pipe "iconv -s -t SJIS | putclip"

